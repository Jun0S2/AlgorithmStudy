# In Python, the -- operator, which typically signifies pre-decrement or post-decrement in languages like C++ or Java, does not exist

str, n = input().strip().split(' ')
n = int(n)

returnValue = ""

while n > 0:
    returnValue+=str
    n -= 1  # Decrement x

print(returnValue)